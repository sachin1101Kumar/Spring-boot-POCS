#To fetch the configurations from spring config server we need to rename the application properties file to bootstarap.properties
#Name of the application should be same as the name of the properties file in git respository
spring.application.name=limits-service

#With this configuration it is going to piuc the default configuration
spring.cloud.config.uri=http://localhost:8888

#changing it to pick a particular profile
spring.profiles.active=dev

